<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="phantom_invoice_form_view" model="ir.ui.view">
        <field name="name">phantom.invoice.form.inherit.phantom.account.bridge</field>
        <field name="model">phantom.invoice</field>
        <field name="inherit_id" ref="phantom_connector.phantom_invoice_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_title')]" position="before">
                <div class="oe_button_box" name="button_box">
                    <button name="action_view_account_move" type="object" class="oe_stat_button"
                        icon="fa-pencil-square-o" invisible="not account_move_id">
                        <field name="account_move_id" widget="statinfo" string="Invoice"/>
                    </button>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
